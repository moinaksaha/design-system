import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import * as colors from '../../dist/dictionary/folkeautomaten/constants.js';

<Meta title="Folkeautomaten|Color System" />

# Palette

<Preview>
	<Story name="Palette">
		<article
			style={{
				display: 'grid',
				gridTemplateColumns: 'repeat(3, 1fr)',
				gridGap: '10px',
				gridAutoRows: 'minmax(100px, auto)'
			}}
		>
			{Object.entries(colors).map(([colorName, colorValue]) => (
				<label>
					{colorName}
					<input type="color" value={colorValue} disabled style={{display: 'block'}} />
					{colorValue}
				</label>
			))}
		</article>
	</Story>
</Preview>
