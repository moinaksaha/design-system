import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import * as colors from '../../dist/dictionary/folkeautomaten/constants.js';

import { Palette } from '../../folkeautomaten/mod.js';

<Meta title="Folkeautomaten|Color System" />

# Palette 

<Preview>
	<Story name="Palette"
		parameters={{ styles: [require('!style-loader!css-loader!sass-loader!../../folkeautomaten/palette-style.scss?story')]}}
	>
		<article
			style={{
				display: 'grid',
				gridTemplateColumns: 'repeat(3, 1fr)',
				gridGap: '10px',
				gridAutoRows: 'minmax(100px, auto)'
			}}
		>
			{Object.entries(colors).map(([colorName, colorValue]) => (
                <Palette 
					className={colorName.toLowerCase().split("_").join("-")} 
					colorName={colorName}
					colorValue={colorValue} 
				/>
			))}
		</article>
	</Story>
</Preview>

