import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import * as colors from '../../dist/dictionary/comeon/constants.js';
import '../../dictionary/comeon/_variables.scss';

import Palette from '../../core/palette/scss/index.js';

<Meta title="Comeon|Palette-SCSS" />

# Palette 

<Preview>
	<Story name="Palette-SCSS">
		<article
			style={{
				display: 'grid',
				gridTemplateColumns: 'repeat(3, 1fr)',
				gridGap: '10px',
				gridAutoRows: 'minmax(100px, auto)'
			}}
		>
			{Object.entries(colors).map(([colorName, colorValue]) => (
                <Palette 
					className={colorName.toLowerCase().split("_").join("-")} 
					colorName={colorName}
					colorValue={colorValue} 
				/>
			))}
		</article>
	</Story>
</Preview>





