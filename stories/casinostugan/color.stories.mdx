import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import * as colors from '../../dist/dictionary/casinostugan/constants.js';

import { Palette } from '../../casinostugan/mod.js';

<Meta title="Casinostugan|Color System" />

# Palette 

<Preview>
	<Story name="Palette"
		parameters={{ styles: [require('!style-loader!css-loader!sass-loader!../../casinostugan/palette-style.scss?story')]}}
	>
		<article
			style={{
				display: 'grid',
				gridTemplateColumns: 'repeat(3, 1fr)',
				gridGap: '10px',
				gridAutoRows: 'minmax(100px, auto)'
			}}
		>
			{Object.entries(colors).map(([colorName, colorValue]) => (
                <Palette 
					className={colorName.toLowerCase().split("_").join("-")} 
					colorName={colorName}
					colorValue={colorValue} 
				/>
			))}
		</article>
	</Story>
</Preview>
